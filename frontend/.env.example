NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api
NEXT_PUBLIC_AUTH_ENABLED=false
# middleware 需要和后端保持一致。
AUTH_COOKIE_NAME=alpha_pulse_session
AUTH_SESSION_SECRET=replace-with-the-same-secret-as-backend
